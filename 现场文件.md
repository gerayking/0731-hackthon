# 现场文件

本文档记录当前现场项目的开发协作规则、待办维护方式与飞书多维表格配置。

## 现场方案说明

当前现场方案由以下文件共同约束：

- `AGENTS.md`：根目录 Agent 宪法，包含第 3 组 North Food 现场开发规则。
- `docs/rfcs/0001-site-constitution.md`：现场方案的设计 RFC。
- `现场文件.md`：本文件，记录飞书多维表格配置和现场流程。
- `skills/north-food-site-constitution/SKILL.md`：供 Codex 使用的现场开发宪法 skill。

## 飞书多维表格

已创建飞书多维表格，用于维护现场开发任务、RFC 记录和认领状态。

- Base 名称：`第 3 组 North Food 开发待办`
- Base URL：https://sxddhcrtbqu.feishu.cn/base/ZkmTb8cl9aqfBesBXmic6qTonqe
- Base Token：`ZkmTb8cl9aqfBesBXmic6qTonqe`
- 数据表：`开发任务`
- 数据表 ID：`tbllF0WGAPYdW1oB`
- 默认视图：`Grid View`
- 视图 ID：`vewQyyhEGm`

### 字段

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| 任务标题 | 文本 | 开发任务或待办事项的简短标题 |
| 任务描述 | 文本 | 任务背景、验收标准、实现说明 |
| 任务类型 | 单选 | `RFC` / `开发` / `测试` / `文档` / `运维` |
| 状态 | 单选 | `待认领` / `已认领` / `进行中` / `待评审` / `已完成` / `已阻塞` |
| 认领人 | 人员 | 当前负责该开发任务的人 |
| 认领时间 | 日期时间 | 任务被认领的时间 |
| 关联 RFC | 文本 | 对应 RFC 编号、标题或链接 |
| 关联文件 | 文本 | 对应代码、文档或现场文件路径 |
| 优先级 | 单选 | `P0` / `P1` / `P2` / `P3` |
| 计划开始时间 | 日期时间 | 计划开始开发的时间 |
| 截止时间 | 日期时间 | 期望完成时间 |
| 验收标准 | 文本 | 完成该任务需要满足的条件 |
| 备注 | 文本 | 补充说明、风险、依赖或后续事项 |

## 现场开发流程

从本文件写入时间起，每次进入开发前必须执行以下流程：

1. 先写 RFC。
   - 新增或修改功能前，先在仓库内创建/更新对应 RFC。
   - RFC 需要说明背景、目标、范围、设计、验收标准和回滚/风险说明。
   - 没有 RFC 的开发任务不应直接进入实现。

2. 将开发任务写入飞书多维表格。
   - 在 `第 3 组 North Food 开发待办` 的 `开发任务` 表中创建记录。
   - `任务标题`、`任务描述`、`关联 RFC`、`关联文件`、`优先级`、`验收标准` 应尽量完整。
   - 初始状态统一设置为 `待认领`。

3. 不同成员认领任务时，必须同步更新多维表格。
   - 将 `状态` 改为 `已认领` 或 `进行中`。
   - 填写 `认领人`。
   - 填写 `认领时间`。
   - 如任务存在阻塞，将 `状态` 改为 `已阻塞`，并在 `备注` 中说明阻塞原因。

4. 开发完成后，在多维表格中关闭任务。
   - 将 `状态` 改为 `待评审` 或 `已完成`。
   - 在 `备注` 中记录实现摘要、验证结果和未解决事项。

5. 后续每次开发前重复上述流程。
   - RFC 是开发前置条件。
   - 飞书多维表格是任务认领和状态流转的唯一现场看板。

## 当前记录

- 已创建 `第 3 组 North Food 开发待办` 多维表格。
- 已写入本现场文件，用于约束后续开发流程。
